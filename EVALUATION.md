# 端到端评测记录

评测使用 `.env` 中的真实 `deepseek-v4-flash`，不是脚本化假模型。评测项目位于 Git 忽略的隔离目录，agent 只能看到工作区内的需求、残缺实现和可见测试；隐藏测试由外部独立执行。

## 评测方法

任务设计参考三类一手资料：

- [Terminal-Bench](https://github.com/harbor-framework/terminal-bench) 将真实终端任务、独立测试和可复现参考解法分离，强调最终环境结果而不是固定行动路径；
- [SWE-bench](https://github.com/SWE-bench/SWE-bench) 采用“仓库 + issue -> 补丁 -> 修复测试与回归测试”的软件工程评测方式；
- OpenAI 对 SWE-bench Verified 的[测试审计](https://openai.com/index/why-we-no-longer-evaluate-swe-bench-verified/)表明，测试不应窄到强制未声明实现，也不应宽到验证题目没有描述的行为。

因此本项目的自建任务满足：规范完整；初始 NOP 基线必须失败；隐藏测试只验证明确要求；可见测试和隐藏 verifier 分离；最终再运行原回归测试。DeepSeek thinking + tools 的多轮消息按[官方协议](https://api-docs.deepseek.com/guides/thinking_mode/)完整回传 `reasoning_content`。

## 项目难度

任务是修复一个多模块 Python Task API，仅允许使用标准库。它同时要求：

- `TaskStore` 的 JSON 持久化、严格结构校验、单调且不复用的 ID；
- 多线程创建与更新不丢数据，写临时文件、`fsync`、原子替换及目录同步；
- `ThreadingHTTPServer` 的健康检查、任务创建/筛选/更新；
- Content-Type、Content-Length、JSON 结构、路径和查询参数的严格验证；
- 400/404/415/500 错误映射，500 不泄露内部异常；
- 可见测试之外的并发、损坏数据和协议边界测试。

初始基线为 4 项可见测试全部失败：3 个 failure、1 个 error。

## 第一轮：发现循环终止缺陷

模型完成项目探索并运行基线测试后，响应达到 8192 输出 token 上限。旧循环看到 `finish_reason=length` 便直接报错，任务在第 4 轮终止，尚未开始修改代码。

这说明复杂任务不能把“输出达到上限”误判为“任务失败”。对应改进：

1. 增加 `AGENT_MAX_OUTPUT_TOKENS`，默认 32768；
2. 达到输出上限时保留 assistant 内容与 `reasoning_content`；
3. 自动追加继续指令，在原有最大轮数预算内恢复，不重复已完成探索；
4. 增加脚本化回归测试验证续写后仍可调用 terminal 并正常结束。

## 第二轮：完整修复与隐藏验收

相同基线重新运行后，agent 用 9 次 terminal 调用完成：读取需求与多模块代码、复现测试、重写存储层和 HTTP 层、运行可见测试，并主动构造额外并发与协议检查。

独立结果：

| 测试 | 结果 |
|---|---|
| 可见功能测试 | 4/4 通过 |
| 隐藏并发与原子写入 | 通过（12 workers、60 次创建、ID 1..60、JSON 完整） |
| 隐藏结构与类型校验 | 通过 |
| 隐藏 HTTP 严格协议 | 通过 |

隐藏测试总计 3/3 通过。没有出现 terminal 无法表达的操作，因此没有增加文件读写或搜索工具。

本轮观察到事件日志在非交互管道中延迟显示，且 terminal 把“工具执行成功”统一显示为 `ok`，无法直观看到命令本身的非零退出码。对应改进：事件强制刷新；terminal 返回结构化的 `exit_code`、`output`、`output_bytes`、`truncated`；完成时汇总轮次、工具调用与 token 使用。

## 第三轮：已有项目上的增量需求

在已通过并发与原子性测试的实现上增加：

- `TaskStore.delete(id)`，删除后 ID 永不复用；
- `DELETE /tasks/{id}`，成功返回 204、空 body、`Content-Length: 0`；
- 非法路径 400、不存在任务 404、存储异常 500。

基线新增测试为 1 failure、1 error。agent 没有推倒重写，而是复用锁与原子保存路径，对两个模块做局部修改。最终统计：13 轮、13 次 terminal 调用、106475 input tokens、4879 output tokens。

独立结果：可见测试 6/6、原隐藏测试 3/3 全部通过；额外验证了并发删除、重启持久化、ID 不复用、204 响应和错误路径。

本轮还发现模型常用 `python ... | tail` 压缩测试输出，而 Bash 默认可能用 `tail` 的 0 覆盖上游测试失败。terminal 因此在 POSIX 下启用 `pipefail`，并新增回归测试确保管道上游失败能够传播。

## 第四轮：并行增量构建引擎

第二个项目要求修复标准库构建引擎，覆盖预执行 DAG 校验、并发调度、失败/跳过传播、输入与依赖 SHA-256 指纹、输出存在性、强制重跑、损坏缓存恢复、原子状态和 CLI 错误码。初始可见测试 4/4 失败。

模型在同一个交互会话中使用 20 轮 terminal：实现功能、自建 21 项补充检查并多次根据非零退出码修复测试。第 20 轮结束时触发硬上限，没有生成最终文字；但独立验证显示实现已经完成：

| 验证 | 结果 |
|---|---|
| 可见 DAG/缓存/CLI/失败传播 | 4/4 通过 |
| 隐藏并发时序与依赖屏障 | 通过 |
| 隐藏输入/命令/输出缓存失效 | 通过 |
| 隐藏路径、损坏状态、原子写、长输出 | 通过 |

随后在同一会话给出 verifier 结果，模型无需工具即可基于此前历史正确总结（54355 input / 808 output tokens），证明跨用户回合历史有效。框架据此改为：轮数上限停止开放 terminal，但额外发起一次 `tool_choice=none` 的收尾请求，要求如实报告未完成项。真实 V4 Flash 在 `AGENT_MAX_ROUNDS=1` 的强制测试中完成一次 terminal 调用并成功自动收尾。

## 第五轮：大输出资源压力

旧 terminal 使用内存管道，生成 32 MB 输出时 agent 进程峰值常驻内存约 114 MB，尽管最终只返回约 20 KB。改为匿名临时文件捕获后，只读取头尾并报告精确 `output_bytes`；相同压力下峰值约 20 MB，返回仍为 `output_bytes=32000000, truncated=true`。超时时也会先终止进程组，再从捕获文件读取有限诊断。

## 最终回归

- 框架单元测试：27/27 通过；
- 高难度项目可见测试：6/6 通过；
- 高难度项目隐藏测试：3/3 通过；
- 并行构建引擎可见测试：4/4 通过；
- 并行构建引擎隐藏测试：3/3 通过；
- 真实 API 轮数上限禁用工具收尾：通过；
- 32 MB terminal 输出有界捕获：通过；
- Python 编译检查通过；
- `.env` 仍由 `.gitignore` 排除。

评测表明，工具数量不是当前瓶颈。真正影响任务完成率的是输出上限恢复、可靠的 shell 退出语义、结构化观察结果、有界资源使用、规划与证据记忆、实时可见性和明确但不丢失最终答复的循环边界。这些能力均可在保留单一 terminal 执行工具的前提下完善。

## 第六轮：规划、上下文与失败恢复

在三类相互独立的隔离项目上使用真实 DeepSeek V4 Flash 进行端到端验证：MiniQueue 持久化队列、长上下文分层配置和重试执行器。三类任务都从残缺实现开始，模型根据任务复杂度通过 `update_plan` 建立并维护计划，再通过 terminal 探索、修改和运行测试；计划工具只维护状态，不执行命令。运行时不强制先调用计划工具，允许 agent 先侦察工作区。

- MiniQueue：可见测试 7/7，独立验证通过。
- 分层配置：可见测试 6/6，独立验证通过；运行中产生约 90K 字符历史，触发上下文压缩，压缩后仍保留完整 tool-call 协议单元和最近证据。
- 重试执行器：可见测试 5/5，独立验证通过。

修复点均由通用信号触发：命令退出码、测试输出、历史长度和完成审计，不包含项目名称、文件名或固定命令分支。复盘还发现，完成审计不应占用最后一个执行轮次；框架现在在已建立计划时预留最后一轮进行审计，避免已完成任务被误标为轮数耗尽。

## 第七轮：流式输出

DeepSeek 请求改为 SSE 流式传输，逐步消费 reasoning、文本和 fragmented tool calls；只有在服务端事件结束后才执行完整工具调用，避免半个 JSON 被误执行。网络中断会在有限次数内重试，终端 UI 同时显示思考增量、回答增量、工具命令和命令结果；`--plain` 保留适合脚本与录屏的纯文本模式。

## 第八轮：从空目录冷启动构建

为避免修复类任务掩盖“从需求到完整产品”的能力，在全空目录中只提供一句高层目标：从零实现完整、可运行的 2048，并自行验证和给出运行说明。没有指定语言、界面、文件结构或测试方案。

第一次运行正常结束，共 23 轮、17 次 terminal、5 个计划项，生成单文件终端游戏和 README。内置自检通过 1916 次断言；外部另用不同实现穷举 625 种行合并，并跑完 50 局随机对局检查步数、计分、方块守恒和终局，全部通过。人工实际输入移动、无效方向、合并、重开和退出也符合规则。完整日志同时暴露出三个效率问题：正式规划前推理过长、为非必要的自动玩家参数投入过多，以及计划完成后的审计重复执行已通过的检查。

第一组改进把计划完成作为审计触发点，记录已有验证证据并要求仅在证据失效或存在具体缺口时复测，同时把系统提示词重写为不含任何评测项目名称或任务分支的通用决策原则。

随后在另一个全空目录以同一句目标冷启动。该次运行证明上下文压缩能在约 169K 原始字符时保持完整协议并继续工作，但也形成了更强的反例：模型把未指定的界面选择扩展为引擎、AI、文本、Curses 和 GUI 多套子系统，27 轮后仍未写 README；API 此时返回 402 余额不足，任务未正常收尾。独立运行其 22 项单元测试均通过，但人工文本模式试玩发现，界面宣称支持 WASD，实际 `w/a/s` 会被判为未知输入。原冒烟命令退出码为 0，却没有检查输出中的错误信息；GUI 冒烟里还出现了恒真的断言。

这个失败没有转化成 2048 专用提示，而是推动了第二组通用改进：

- 默认推理强度由 high 调整为 medium，上下文预算由 300K 收紧到 120K 字符，并保留环境变量覆盖；
- 两次初始 terminal 观察后只提醒模型自主判断是否需要计划，不强制把计划作为第一步；
- 建立计划后，每六次 terminal 执行若未更新进度，触发一次基于原始验收标准的范围与状态检查；
- 明确“完备”是满足全部需求而不是最大化功能数，未指定的设计空间选择一个最小且一致的方案；
- 验证必须检查真实输出和副作用，并使用在行为错误时能够失败的断言，不能把退出码 0 当作所有行为正确的证明。

框架回归现为 35/35 通过，新增测试覆盖规划提醒、计划停滞检查点、完成审计复用已有证据、通用验证命令识别和推理配置校验。由于真实 API 余额已不足，第二组改进尚未进行第三次线上冷启动；这一限制在结论中保留，不用脚本化假模型冒充真实端到端结果。
